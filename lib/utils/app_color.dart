import 'package:flutter/material.dart';

class AppColor {
  static Color primary = Colors.teal;
  static Color snackbarBackground = Colors.teal[100]!;
  static Color disabledBackground = Colors.grey.withOpacity(0.1);
  static Color disabled = Colors.grey.withOpacity(0.8);
}
